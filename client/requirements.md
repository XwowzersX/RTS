## Packages
react-use-gesture | For handling complex map panning/zooming interactions if needed
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging Tailwind classes

## Notes
WebSocket connects to /ws (derived from window.location.protocol/host)
Game rendering uses HTML5 Canvas API for performance
Assets are procedural/shapes for MVP
